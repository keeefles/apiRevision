exports.subtraction = (a, b) =>{
    return b > a ? 
    `${b} is greater than ${a}` : a - b 
}
// if b is greater than a, then it will display as (b is greater than a) but if a is greater than b, it will calculate.